# Supabase (deprecated for MVP)

The SQL migrations in this folder are **not** the source of truth for the MVP persistence model.

BaladiGuard MVP uses **Amazon DynamoDB**. See [docs/database.md](../docs/database.md) for the canonical ticket and entity model aligned with the API contract.

These files are kept for historical reference only and should not be extended unless the storage decision changes.
